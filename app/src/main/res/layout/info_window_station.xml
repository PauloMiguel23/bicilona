<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_info_window"
    android:padding="10dp"
    android:minWidth="180dp">

    <!-- Station name -->
    <TextView
        android:id="@+id/tvInfoTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="#1A1A2E"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Capacity bar -->
    <ProgressBar
        android:id="@+id/capacityBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginTop="6dp"
        android:progressDrawable="@drawable/bg_capacity_bar"
        android:max="100"
        android:progress="50" />

    <TextView
        android:id="@+id/tvCapacityLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="#999999"
        android:layout_marginTop="2dp"
        android:gravity="center" />

    <!-- Bike counts: two equal columns -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="6dp">

        <!-- Mechanical -->
        <TextView
            android:id="@+id/tvInfoMechanical"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textSize="13sp"
            android:textStyle="bold" />

        <!-- Electric -->
        <TextView
            android:id="@+id/tvInfoElectric"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textSize="13sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Docks -->
    <TextView
        android:id="@+id/tvInfoDocks"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="11sp"
        android:textColor="#757575" />

</LinearLayout>
